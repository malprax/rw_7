<%= form_for(@rukunwarga) do |f| %>
  <% if @rukunwarga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rukunwarga.errors.count, "error") %> prohibited this rukunwarga from being saved:</h2>

      <ul>
      <% @rukunwarga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
