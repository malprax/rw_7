<%= form_for(@rukuntetangga) do |f| %>
  <% if @rukuntetangga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rukuntetangga.errors.count, "error") %> prohibited this rukuntetangga from being saved:</h2>

      <ul>
      <% @rukuntetangga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
